package com.evantis.model;

// The two service categories on the Evantis platform.
// Add new categories here as the marketplace grows (e.g. CATERING, MUSIC).
public enum VendorCategory {
    PHOTOGRAPHY,
    EVENT_DECORATION
}
